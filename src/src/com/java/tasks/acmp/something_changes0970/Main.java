package com.java.tasks.acmp.something_changes0970;

public class Main {
    public static void main(String[] args) {
        Changes changes = new Changes();
        changes.add(changes.getAll(), changes.getL());
        changes.result(changes.getL(),changes.getAll());
    }
}
